{
  "name": "vanatur",
  "version": "1.0.0",
  "description": "Multi-page VANATUR website with Supabase backend",
  "private": true,
  "scripts": {
    "dev": "npx serve public -l 3000 --no-clipboard",
    "build": "echo 'Static site - no build step required'",
    "start": "npx serve public -l 3000",
    "start:python": "python3 server.py",
    "dev:python": "python3 dev-server.py",
    "db:bootstrap": "python3 scripts/bootstrap_postgres.py",
    "db:migrate-data": "python3 scripts/migrate_data_to_postgres.py"
  },
  "devDependencies": {},
  "dependencies": {}
}
